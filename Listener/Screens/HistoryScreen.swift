//
//  HistoryScreen.swift
//  Listener
//
//  Created by jamie baddeley on 11/04/2026.
//

import SwiftUI

// Placeholder shell. The real list, pagination, swipe-to-delete, and
// detail navigation land in a follow-up commit. Lives here now so the
// navigation shell in `ListenScreen` has somewhere to push to.
struct HistoryScreen: View {
    var body: some View {
        Text("History coming next")
            .foregroundStyle(.secondary)
            .navigationTitle("History")
            .navigationBarTitleDisplayMode(.inline)
    }
}
